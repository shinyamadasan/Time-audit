node_modules/
android/
.claude/
claude-obsidian/
resources/
*.log
*.tmp
.DS_Store
test-results/
playwright-report/
# Audio served from GitHub Pages — not bundled in APK
www/Sounds/

# AI Dev OS runtime artifacts -- these MUST be ignored, or the merge gates see a
# permanently dirty tree and refuse to merge anything.
automation.lock
.last-phase-result.txt
.scope-note.txt

# Phase 10 — machine-specific Life Ledger background sync config (real local paths).
# Commit only scripts/life-ledger-sync-worker.config.example.json.
scripts/life-ledger-sync-worker.config.json
